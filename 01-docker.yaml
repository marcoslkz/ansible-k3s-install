- hosts: all
  become: yes
  roles:
    - geerlingguy.docker
  tasks:
  - name: install pre-requisites
    pip:
      name:
        - openshift
        - pyyaml
        - kubernetes 
        - python-setuptools
