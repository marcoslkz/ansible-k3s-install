apiVersion: postgresql.cnpg.io/v1
kind: DatabaseRole
metadata:
  name: keycloak-role
  namespace: cnpg-system
spec:
  cluster:
    name: db
  name: keycloak_user
  login: true
  passwordSecret:
    name: keycloak-user-secret
    key: password
