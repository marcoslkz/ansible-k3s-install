apiVersion: postgresql.cnpg.io/v1
kind: Role
metadata:
  name: keycloak-role
  namespace: cnpg-system
spec:
  cluster:
    name: db
  name: keycloak_user
  passwordSecret:
    name: keycloak-user-secret
    key: password